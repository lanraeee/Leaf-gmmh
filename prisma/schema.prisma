generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// ─── Enums ────────────────────────────────────────────────────────────────────

enum Role {
  NURSE
  SENIOR_NURSE
  CHARGE_NURSE
  ADMIN
}

enum LegalStatus {
  VOLUNTARY
  INVOLUNTARY_SECTION
  COMMUNITY_ORDER
}

enum LeaveType {
  UNESCORTED
  ESCORTED
  THERAPEUTIC_LEAVE
  OVERNIGHT
  EXTENDED
}

enum LeaveStatus {
  PENDING_APPROVAL
  APPROVED
  ON_LEAVE
  RETURNED
  OVERDUE
  AWOL
  CANCELLED
}

enum ConsentStatus {
  CONSENTED
  DECLINED
  NOT_APPLICABLE
}

enum AlertType {
  OVERDUE
  APPROACHING_DUE
  NOT_RETURNED
  INCIDENT
}

enum AlertSeverity {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

// ─── Core Entities ────────────────────────────────────────────────────────────

model Ward {
  id          String   @id @default(cuid())
  name        String
  code        String   @unique
  location    String
  phone       String?
  email       String?
  description String?
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @default(now()) @updatedAt

  units        WardUnit[]
  staff        Staff[]
  patients     Patient[]
  leaveRecords LeaveRecord[]
}

model WardUnit {
  id          String   @id @default(cuid())
  wardId      String
  name        String
  code        String
  description String?
  bedCount    Int?
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())

  ward        Ward     @relation(fields: [wardId], references: [id])
  patients    Patient[]

  @@unique([wardId, code])
}

model Staff {
  id            String   @id @default(cuid())
  azureId       String?  @unique
  name          String
  email         String   @unique
  role          Role     @default(NURSE)
  pinHash       String?
  isActive      Boolean  @default(true)
  wardId        String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  ward              Ward?             @relation(fields: [wardId], references: [id])
  initiatedLeaves   LeaveRecord[]     @relation("InitiatedBy")
  approvedLeaves    LeaveApproval[]
  returnedLeaves    LeaveRecord[]     @relation("ReturnedBy")
  awolEscalations   AwolEscalation[]
  auditLogs         AuditLog[]
  pushSubscriptions PushSubscription[]
  sessions          Session[]
  accounts          Account[]
}

model Patient {
  id              String       @id @default(cuid())
  // PARIS fields — populated from PARIS when available, manual entry until then
  parisId         String?      @unique
  mrn             String       @unique
  firstName       String
  lastName        String
  dateOfBirth     DateTime
  gender          String?
  legalStatus     LegalStatus  @default(VOLUNTARY)
  wardId          String
  unitId          String?
  consultantName  String?
  admissionDate   DateTime?
  riskLevel       String?      @default("LOW")
  // PARIS sync metadata
  parisLastSync   DateTime?
  parisSource     Boolean      @default(false)
  isActive        Boolean      @default(true)
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt

  ward            Ward         @relation(fields: [wardId], references: [id])
  unit            WardUnit?    @relation(fields: [unitId], references: [id])
  leaveRecords    LeaveRecord[]
}

// ─── Leave Records ────────────────────────────────────────────────────────────

model LeaveRecord {
  id                String       @id @default(cuid())
  patientId         String
  wardId            String
  initiatedById     String
  returnedById      String?

  leaveType         LeaveType
  status            LeaveStatus  @default(PENDING_APPROVAL)

  destination       String
  destinationDetail String?
  escortName        String?
  escortPhone       String?

  departureTime     DateTime?
  agreedReturnTime  DateTime?
  proposedReturnTime DateTime?
  isTimeAgreed      Boolean      @default(false)
  actualReturnTime  DateTime?

  returnConditionNotes String?
  incidentOnReturn     Boolean  @default(false)
  incidentDetail       String?

  createdAt         DateTime     @default(now())
  updatedAt         DateTime     @updatedAt

  patient           Patient        @relation(fields: [patientId], references: [id])
  ward              Ward           @relation(fields: [wardId], references: [id])
  initiatedBy       Staff          @relation("InitiatedBy", fields: [initiatedById], references: [id])
  returnedBy        Staff?         @relation("ReturnedBy", fields: [returnedById], references: [id])
  approval          LeaveApproval?
  consent           PatientConsent?
  voiceRecording    VoiceRecording?
  appearance        PatientAppearance?
  awolEscalation    AwolEscalation?
  alerts            Alert[]
  auditLogs         AuditLog[]
}

model LeaveApproval {
  id              String   @id @default(cuid())
  leaveRecordId   String   @unique
  approvedById    String
  method          String   // "PIN" | "SIGNATURE"
  signatureData   String?  @db.Text
  approvedAt      DateTime @default(now())
  notes           String?

  leaveRecord     LeaveRecord @relation(fields: [leaveRecordId], references: [id])
  approvedBy      Staff       @relation(fields: [approvedById], references: [id])
}

model PatientConsent {
  id              String        @id @default(cuid())
  leaveRecordId   String        @unique
  status          ConsentStatus
  consentedAt     DateTime?
  declineReason   String?
  witnessedBy     String?

  leaveRecord     LeaveRecord   @relation(fields: [leaveRecordId], references: [id])
}

// ─── Voice & Media ────────────────────────────────────────────────────────────

model VoiceRecording {
  id              String   @id @default(cuid())
  leaveRecordId   String   @unique
  filePath        String
  durationSeconds Int?
  transcript      String?  @db.Text
  transcriptedAt  DateTime?
  mimeType        String   @default("audio/webm")
  sizeBytes       Int?
  createdAt       DateTime @default(now())

  leaveRecord     LeaveRecord @relation(fields: [leaveRecordId], references: [id])
}

model PatientAppearance {
  id              String   @id @default(cuid())
  leaveRecordId   String   @unique
  photoPath       String?
  faceBlurred     Boolean  @default(true)

  head            String?
  upperBody       String?
  lowerBody       String?
  footwear        String?
  accessories     String?
  additionalNotes String?

  capturedAt      DateTime @default(now())

  leaveRecord     LeaveRecord @relation(fields: [leaveRecordId], references: [id])
}

// ─── AWOL Escalation ─────────────────────────────────────────────────────────

model AwolEscalation {
  id              String   @id @default(cuid())
  leaveRecordId   String   @unique
  escalatedById   String
  notifiedStaff   String?
  policeContacted Boolean  @default(false)
  policeIncidentNo String?
  notes           String?  @db.Text
  // InPhase incident reference
  inphaseRef      String?
  inphaseStatus   String?
  escalatedAt     DateTime @default(now())

  leaveRecord     LeaveRecord @relation(fields: [leaveRecordId], references: [id])
  escalatedBy     Staff       @relation(fields: [escalatedById], references: [id])
}

// ─── Alerts ───────────────────────────────────────────────────────────────────

model Alert {
  id              String        @id @default(cuid())
  leaveRecordId   String
  type            AlertType
  severity        AlertSeverity @default(MEDIUM)
  message         String
  isAcknowledged  Boolean       @default(false)
  acknowledgedBy  String?
  acknowledgedAt  DateTime?
  createdAt       DateTime      @default(now())

  leaveRecord     LeaveRecord   @relation(fields: [leaveRecordId], references: [id])
}

// ─── Audit Trail ──────────────────────────────────────────────────────────────

model AuditLog {
  id              String   @id @default(cuid())
  staffId         String
  leaveRecordId   String?
  action          String
  detail          String?  @db.Text
  ipAddress       String?
  userAgent       String?
  createdAt       DateTime @default(now())

  staff           Staff        @relation(fields: [staffId], references: [id])
  leaveRecord     LeaveRecord? @relation(fields: [leaveRecordId], references: [id])
}

// ─── Push Notifications ───────────────────────────────────────────────────────

model PushSubscription {
  id          String   @id @default(cuid())
  staffId     String
  endpoint    String   @unique
  p256dh      String
  auth        String
  userAgent   String?
  createdAt   DateTime @default(now())

  staff       Staff    @relation(fields: [staffId], references: [id], onDelete: Cascade)
}

model NotificationLog {
  id          String   @id @default(cuid())
  staffId     String?
  wardId      String?
  channel     String   // "push" | "email"
  type        String   // "AWOL" | "OVERDUE" | "ALERT" | "RETURN"
  subject     String
  recipientRef String? // email address or push endpoint prefix
  success     Boolean
  error       String?
  sentAt      DateTime @default(now())
}

// ─── NextAuth Tables ─────────────────────────────────────────────────────────

model Account {
  id                 String  @id @default(cuid())
  userId             String
  type               String
  provider           String
  providerAccountId  String
  refresh_token      String? @db.Text
  access_token       String? @db.Text
  expires_at         Int?
  token_type         String?
  scope              String?
  id_token           String? @db.Text
  session_state      String?
  user               Staff   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         Staff    @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}
